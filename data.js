// Legacy catalog data is intentionally unused by the rebuilt one-product site.
// Kept as an empty compatibility file so older static hosts do not 404 if cached HTML requests it.
window.JINNAMEES_DATA = {
  business: {},
  menu: []
};
